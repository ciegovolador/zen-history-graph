## 1. Git Hook

- [x] 1.1 Create `.githooks/pre-push` script that rejects pushes to `main` or `master` with an actionable error message
- [x] 1.2 Make the hook executable (`chmod +x`)

## 2. Automatic Setup

- [x] 2.1 Add `postinstall` script to `package.json` that runs `git config core.hooksPath .githooks`

## 3. Documentation

- [x] 3.1 Create `CONTRIBUTING.md` with branch workflow, hook explanation, and local setup instructions
